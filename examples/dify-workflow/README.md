# Dify Workflow Example

This example models a workflow that starts as public FAQ answering but drifts into sensitive support-record export.

Approved action:

```text
Answer from public FAQ content.
```

Executed action:

```text
Export recent support records to an external webhook.
```

Security lesson:

```text
Workflow accessibility and low-code convenience do not remove the need for data-boundary checks.
```

