# Browser Agent Example

This example models a browser agent operating in a customer-support workflow.

Approved action:

```text
Draft an internal support note.
```

Executed action:

```text
Send a customer-facing email.
```

Security lesson:

```text
The difference between a draft and a sent message is an action boundary, not a UI detail.
```

